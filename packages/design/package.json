{
  "name": "@dezin/design",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bundled Dezin design systems (9-section DESIGN.md + tokens) and a registry. Default = modern-minimal (Linear/Vercel).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings --test 'test/*.test.ts'",
    "test:coverage": "node --experimental-strip-types --no-warnings --experimental-test-coverage --test-coverage-include='src/**/*.ts' --test-coverage-exclude='**/*.test.*' --test-coverage-lines=93 --test-coverage-branches=71 --test-coverage-functions=88 --test 'test/*.test.ts'"
  }
}
