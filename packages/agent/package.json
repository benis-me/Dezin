{
  "name": "@dezin/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent runner abstraction + generateArtifact(): wires the @dezin/quality lint→repair closed loop into a runner-driven generation.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings --test 'test/*.test.ts'",
    "test:coverage": "node --experimental-strip-types --no-warnings --experimental-test-coverage --test-coverage-exclude='**/*.test.*' --test-coverage-lines=69 --test-coverage-branches=70 --test-coverage-functions=66 --test 'test/*.test.ts'"
  },
  "dependencies": {
    "@dezin/quality": "workspace:*"
  }
}
